# Быстрый старт - Библиотека валидации BeHappy

## 🚀 Минимальный пример (3 шага)

### 1. HTML - структура формы

```html
<form novalidate>
  <label for="name">Имя</label>
  <input type="text" id="name" name="name" required minlength="3">
  <p role="alert" aria-live="assertive"></p>
  <button type="submit">Отправить</button>
</form>
```

**Важно:**
- `novalidate` - отключает стандартную валидацию браузера
- `name="name"` - имя поля (обязательно!)
- `role="alert"` - место для показа ошибок

### 2. TypeScript - настройка валидации

```typescript
import * as b from "./index";

const form = document.querySelector("form");
const validator = b.form(form);

// Настраиваем правила
validator.field("name").string().required("Имя обязательно!").min("Минимум 3 символа!");

// При отправке формы
form.addEventListener("submit", (event) => {
  event.preventDefault();
  validator.validate();
});
```

### 3. Готово! 🎉

Теперь при отправке формы автоматически проверяются правила и показываются ошибки.

---

## 🎯 Валидация конкретных полей

Теперь вы можете валидировать только те поля, которые нужно:

```typescript
// Валидировать все поля
validator.validate();

// Валидировать только определенные поля
validator.validate(["name", "email"]);

// Валидировать одно конкретное поле
const result = validator.validateField("name");
if (!result.isValid) {
  console.log(result.error); // "Имя обязательно!"
}
```

**Примеры использования:**

```typescript
// При отправке формы - проверяем все поля
form.addEventListener("submit", (event) => {
  event.preventDefault();
  const result = validator.validate();
  if (result.isValid) {
    // Отправляем форму
  }
});

// При потере фокуса - проверяем только это поле
nameInput.addEventListener("blur", () => {
  validator.validateField("name");
});

// Проверяем несколько полей перед отправкой
const result = validator.validate(["name", "email", "age"]);
```

## 📋 Шпаргалка по методам

### Строки (string)

```typescript
validator.field("name").string()
  .required("Обязательно")           // Поле обязательно
  .min("Минимум 3 символа")          // Из атрибута minlength
  .max("Максимум 50 символов")       // Из атрибута maxlength
  .email("Некорректный email")       // Для type="email"
  .pattern(/^[A-Z]/, "С заглавной")  // Регулярное выражение
```

### Числа (number)

```typescript
validator.field("age").number()
  .required("Обязательно")
  .min("Минимум 18")                 // Из атрибута min
  .max("Максимум 100")                // Из атрибута max
  .positive("Должно быть > 0")
  .integer("Только целое число")
```

### Массивы / Чекбоксы (array)

```typescript
validator.field("interests").array()
  .required("Выберите хотя бы один")
  .min("Минимум 2")                  // Из data-min
  .max("Максимум 5")                 // Из data-max
```

---

## 🎯 Как это работает (простыми словами)

```
1. b.form(form)
   ↓
   Находит все поля в форме
   Сохраняет информацию о них

2. validator.field("name").string().required()
   ↓
   Сохраняет правила валидации в памяти

3. validator.validate()
   ↓
   Проверяет все правила
   Показывает ошибки в контейнерах
   Возвращает результат
```

---

## 💡 Частые вопросы

**Q: Почему ошибки не показываются?**
A: Проверьте, что есть элемент с `role="alert"` после поля.

**Q: Как отключить валидацию для поля?**
A: Просто не вызывайте `validator.field()` для этого поля.

**Q: Можно ли валидировать без HTML атрибутов?**
A: Да, но правила min/max/minlength/maxlength читают значения из атрибутов.

**Q: Как получить результат валидации?**
A: `const result = validator.validate();` вернет `{ isValid: boolean, errors: {...} }`

---

## 📖 Полная документация

Смотрите `README_VALIDATOR.md` для подробного объяснения.
